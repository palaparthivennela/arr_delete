#include<bits/stdc++.h>
#include<vector>
using namespace std;
int main()
{
	int n,cap,pos=0,ele,a[cap];
	cout<<"Enter the array size:";
	cin>>cap;
	cout<<"Enter no.of elements:";
	cin>>n;
	for(int i=1;i<=n;i++)
		cin>>arr[i];
	cout<<"Enter the value to be deleted";
	cin>>ele;
	for(int i=1;i<=n;i++)
		if(a[i]==ele)
			pos=i;
	if(pos==0)
	{
	    cout<<"Element not found in array";
	}
	else
	{
	    for(int i=pos;i<=cap;i++)
		    a[i]=a[i+1];
	    n--;
	    for(int i=1;i<=n;i++)
		    cout<<a[i]<<" ";
	}
}
